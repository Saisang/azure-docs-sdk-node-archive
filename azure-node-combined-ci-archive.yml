trigger:
  batch: true
  branches:
    include:
    - main
  paths:
    include:
    - metadata/legacy
    - ci-configs/packages-legacy.json
    
resources:
  repositories:
    - repository: ReferenceAutomation
      type: git
      name: Content CI/ReferenceAutomation
      ref: refs/heads/sai-testjumppipeline

extends:

  template: nodejs/pipeline-yaml/azure-node-combined-ci-archive.yml@ReferenceAutomation
